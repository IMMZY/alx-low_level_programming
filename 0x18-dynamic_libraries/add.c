#include <stdio.h>
#include <stdlib.h>

/**
 * add - add two integers
 * @a: first integer
 * @b: second integer
 * Return: addition of two integers result
 */

int add(int a, int b)
{
	return(a + b);
}
