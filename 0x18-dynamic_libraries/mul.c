#include <stdio.h>
#include <stdlib.h>

/**
 * mul - multiply two integers
 * @a: first integer
 * @b: second integer
 * Return: multiplication final result
 */

int  mul(int a, int b)
{
	return(a * b);
}
